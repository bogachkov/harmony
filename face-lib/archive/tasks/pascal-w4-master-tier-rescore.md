# pascal-w4-master-tier-rescore

Q1-W4 Box 3 — the EVIDENCE deliverable, and the W4 headline. After the
ceiling-raisers land (`tasks/felix-nick-timmflat-ceiling-raisers.md`),
re-score timmFlat against the AGENTS.md anchor table and answer the one
question Gary asked W4 to answer: **did the engine reach confident-pro
(Pascal-6+) on timmFlat — yes or no, on how many cells, and why?**

## Brief

Gary confirmed "prove master-tier" as the Q1 bar. timmFlat closed W3 at
16/16 ≥ Pascal-5 (off-day-pro). W4 landed the ceiling-raisers (expected:
`highlightCutout` + per-feature line-weight multiplier — confirm against
the actual landed list in the Felix+Nick handoff). Your score IS the
evidence. Both outcomes are legitimate and close the box:

- **≥6 on a meaningful share of cells** → "the engine CAN reach
  confident-pro; master-tier is on the surface." The high-value
  positive result.
- **Capped at 5 even with the raisers** → "the current ceiling is 5;
  here is the named root cause" (most likely the features-as-decals
  integration debt — the orbital-socket / attachment-plane architecture
  timmFlat normally dodges; see `research/leo-face-integration-audit.md`
  + the Box 1 audit's ceiling call). This NEGATIVE result is equally
  valuable — it de-risks Q2/Q3 by telling us the clothed-character
  ceiling won't be higher than the face ceiling, and it feeds Lloyd's
  Q2-open body-architecture pass.

**Frame the verdict as "we know whether the engine reaches confident-pro
and why," NOT "timmFlat is Pascal-7 or W4 failed."**

## Concrete deliverable

Append a Pass 4 section to `face-lib/research/pascal-w2-timmflat.md`
(the durable timmFlat scoring file). Required:

1. **Full 16-cell re-score**, absolute against the anchor table. Per
   cell: yes/no/almost + one-line read + the score. Call out which
   cells the highlightCutout / line-weight raisers moved, and by how
   much. Hero register lives here.
2. **Villain-register probe scores** — the off-grid probes the Box 1
   audit named (`adultFemPointed`, `elderMascPear`, the Joker-direction
   lid-weight probe). The line-weight multiplier's lift shows up HERE,
   not on the hero grid. Score each absolute.
3. **The headline verdict.** A plain yes/no: did any cell reach
   Pascal-6+? How many? What's the new ceiling, and what's the named
   root cause if it's still 5? This is the sentence Bob carries to Gary.
4. **Mixture-rule guard.** Re-confirm `tintin × 4` regression HOLDS —
   the line-weight multiplier touches a shared render path; verify it
   did not drift the non-opted packs. If tintin slides, that's a
   mixture-rule violation to flag, not a score to soften.
5. **Sniff-test sweep** on any ≥6 cell, harder than usual. AGENTS.md:
   "If Pascal scores above 5 on output that has dead procedural hair,
   flat construction-line strokes, or features that don't integrate
   into a single drawn-feeling whole — Pascal is wrong. Calibrate
   HARDER, not softer." A cel-shadow cutout that reads as a flat
   procedural overlay rather than a drawn shadow does NOT earn a 6.
   The bar for 6-7 is "I'd drop this into my game without side-eye"
   (Gary's gut bar), not "it improved since Pass 3."

## Acceptance

- 16-cell grid + villain probes scored absolute, per-cell, with the
  raiser-attributed movement called out.
- A clear yes/no/how-many headline on Pascal-6+ reachability, with the
  why (the named root cause if capped at 5).
- `tintin × 4` mixture-rule guard re-confirmed.
- Sniff-test sweep on every ≥6 cell; no soft 6s.
- Movement signal vs Pass 3 (separate line, NOT folded into the score).

## Notes

- Do NOT re-litigate calibration (closed Pass 1) or the four-corner
  test (closed Pass 3, demographic-topology gap resolved).
- You score at most once per substantive change — this is the one W4
  evidence pass. Don't ask Felix/Nick to "fix toward" a higher score;
  score honestly and name the ceiling. Preference enforcement is not
  your job; finding the ceiling is.
- The user would rather hear "still capped at 5" honestly than "close
  enough to 6" when it isn't. A truthful 5-cap is the win condition if
  that's the reality — it's the exact information the Q2 decision needs.

## Handoff

(Pascal fills in on completion. The headline yes/no/why is what Bob
relays to Gary and what Claudia plans Q2-open against.)
