# claudia-q1w3-close-w4-plan

Claudia's fifth spawn. Close Q1-W3 (clean, 16/16). Open Q1-W4 against
David's directional call — Gary confirmed the quality-bar judgment.

## Brief

W3 closed CLEAN: Pascal Pass 3 scored the full timmFlat 16-cell grid
at **16/16 ≥ Pascal-5**, four-corner passes, tintin × 4 regression
holds. All three engine rows (Q2 demographic-topology, Q1 cascade-
merge manifest, Felix long-hair primitive) landed + Lloyd-approved.

David's W4 directional review (`tasks/david-q1-w3close-directional-
review.md`, commit `634af04`) set the direction:

- **Trajectory: ON TRACK, met at the floor.** Q1 goal (N≥4 packs at
  quality, depth>count) satisfied as written — 4 packs, timmFlat
  16/16. Caveat: met at Pascal-5 (off-day-pro), not the Pascal-7
  working-pro aspiration.
- **W4 direction: (B) primary ~60% + (C) ~40%; (A) pack #5 deferred
  to Q2-open.**

**Gary's escalation answer (the decision David kicked up): "Prove
master-tier."** Gary confirmed master-tier IS the bar for Q1. This
flips (B) from nice-to-have to **must-have**. W4's primary job is to
prove the engine CAN reach Pascal 6-7 on timmFlat — not just that it
makes off-day-pro packs.

## Your task

1. **Close Q1-W3 in SPRINT.md.** Archive to `## History` with honest
   notes: 16/16 strict close; the three engine rows; Lloyd Pass 4+5
   APPROVED-AS-IS; Pascal Pass 3 clean-close; Felix's first delivery
   + day-one calibration; tintin held. All W3 boxes `[x]`.

2. **Open Q1-W4 against David's direction + Gary's confirmation.**
   - **Primary (B): push timmFlat Pascal-5 → 6-7.** This is now the
     must-have, not 60/40. The BACKLOG ceiling-raisers are the fuel:
     `highlightCutout` primitive (Pascal ~7→8 per BACKLOG),
     per-feature line-weight multiplier (upper-lid 2-3× face contour
     — Timm canon; villain register reaches ~8 with it). Pull what
     the master-tier push needs. This is graphics-primitive +
     pedagogy work — Felix + Leo + Nick territory; Pascal scores the
     lift. **The deliverable is EVIDENCE: does the engine reach
     confident-pro on timmFlat, yes or no?** A negative result
     (engine can't get past ~6 without architectural change) is a
     legitimate, valuable W4 outcome — it de-risks Q2/Q3.
   - **Secondary (C): Holly's first spawn — test-strategy doc.**
     Deferred since W1. Must land before Q2 triples the surface
     (bodies/clothes/poses). Plus thin regression coverage + BACKLOG
     triage if W4 has room.
   - **(A) pack #5: deferred to Q2-open, NOT cut.** Per David: spec
     it full-stack with body conventions at Q2-open rather than
     re-opening a face-only pack. Confirm it stays filed.

3. **Spawn order for Bob.** Sequence the master-tier push. Likely
   shape (your call): Leo/Rollo name what "confident-pro timmFlat"
   needs (a short spec or audit) → Felix/Nick implement the ceiling-
   raisers → Pascal re-scores for the lift. Holly's test-strategy
   doc can run parallel (zero code overlap). Decide the waves.

4. **BACKLOG curation.** Promote the ceiling-raisers you're pulling
   into W4; keep the rest filed; confirm pack #5 candidates + the
   Q2-open full-stack-spec note are filed.

5. **Plain English.** Keep the "ship"→"land/close/prove" cleanup
   going per Gary's earlier flag.

## Constraints

- Q1 envelope: W4 is the LAST week of Q1. If (B)+(C) can't both
  fully fit, (B) is the must-have (Gary's call); (C) Holly's
  test-strategy DOC is the minimum (the regression coverage can
  slip to Q2-open if needed). Name what slips.
- The (B) deliverable is evidence, not a guaranteed Pascal-7. Frame
  the W4 acceptance as "we know whether the engine reaches confident-
  pro and why," not "timmFlat is Pascal-7 or W4 failed."
- Mixture rule. Ceiling-raisers are additive primitives — existing
  packs stay byte-identical (default/tintin/ligneClaire); timmFlat
  is the only pack that opts into them.
- You plan; Bob spawns; David set direction; Gary confirmed the bar.

## Deliverable

- Updated `face-lib/SPRINT.md` — W3 → History, W4 active with goal +
  acceptance + queue.
- Updated `face-lib/BACKLOG.md` — promotions/deferrals.
- 1-3 new task files in `face-lib/tasks/` for the W4 spawns.
- Updated `## Handoff` here with Bob's spawn order.
- Return note (<200 words) to Bob.

## Context

- `face-lib/ROADMAP.md` — David's W4-shape + Q1→Q2 handoff updates
  landed `634af04`.
- `face-lib/AGENTS.md` — 10-role crew (Felix included).
- `face-lib/SPRINT.md` — W3 close state.
- `face-lib/research/pascal-w2-timmflat.md` §Pass 3 — W3 scores +
  the ceiling note (the master-tier target Pascal is scoring against).
- `face-lib/research/stylepack-timmFlat-spec.md` — the original pack
  spec; §3 names the ceiling-raisers Leo+Rollo flagged.
- `face-lib/BACKLOG.md` — ceiling-raiser rows (highlightCutout,
  per-feature line-weight multiplier) with their Pascal-lift
  estimates.
- `face-lib/tasks/david-q1-w3close-directional-review.md` — David's
  full directional reasoning.

## Handoff

(Claudia fills in on completion.)
